{
  "name": "pagenry",
  "version": "0.1.3",
  "description": "Detecting page changes every interval of time (default 1min) to detect if page content has changed then it sends an email if change has been found",
  "main": "pagenry.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brent Ertz <m.amged@hotmail.com> (@mamged/)",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^1.5.2",
    "nodemailer": "^2.3.2"
  },
  "keywords": [
    "page",
    "pagenry",
    "watch page",
    "monitor page",
    "detect page chage"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/mAmged/pagenry.git"
  },
  "bugs": {
    "url": "https://github.com/mAmged/pagenry/issues"
  }
}
